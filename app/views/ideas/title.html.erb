<%= simple_form_for(
  current_idea,
  :url => idea_update_path(current_idea, params[:step])
) do |f| %>
  <%= f.input(
    :title,
    :label => 'Does your idea have a name?',
    :required => true
  ) %>
  <%= idea_redirect_field(f, :description) %>
  <%= f.submit "Next" %>
<% end %>
